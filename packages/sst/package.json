{
  "name": "@monorise/sst",
  "version": "4.3.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/monorist/monorise.git",
    "directory": "packages/sst"
  },
  "description": "Monorise SST v4 module — API, DynamoDB table, event bus, processors, dashboards, and Athena analytics infrastructure",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "sst:install": "npx sst install",
    "build": "tsup && tsc --emitDeclarationOnly --outDir dist || true",
    "dev": "tsup --watch"
  },
  "files": [
    "dist"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "sst": "^4.7.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.149"
  }
}
